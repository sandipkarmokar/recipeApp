import React from 'react';

import image from '../components/food.jpg';

const ListView = () => {
    return (
        <>
            <img src={image} alt="" />
        </>
    );
};

export default ListView;
